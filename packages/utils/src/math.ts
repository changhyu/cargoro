/**
 * Utils 패키지의 빌드 출력을 보장하기 위한 파일입니다.
 */
export const UTILS_VERSION = '1.0.0';

/**
 * 간단한 더하기 함수
 */
export function add(a: number, b: number): number {
  return a + b;
}

/**
 * 간단한 빼기 함수
 */
export function subtract(a: number, b: number): number {
  return a - b;
}
