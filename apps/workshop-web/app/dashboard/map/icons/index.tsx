'use client';

// 필요한 아이콘만 개별적으로 import
import { Truck, MapPin, Navigation, LocateFixed, Info, AlertCircle } from 'lucide-react';

// 아이콘을 개별적으로 export
export { Truck, MapPin, Navigation, LocateFixed, Info, AlertCircle };

// 기본 아이콘 컴포넌트
const Icon = Truck;
export default Icon;
